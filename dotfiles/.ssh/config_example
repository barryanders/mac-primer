# Rename this file to `config`.
# Replace the following example with your own.
# With this example, `ssh server` would connect you to
# `me@mysite.web` using the public key, `example_rsa.pub`

Host server
  User me
  HostName mysite.web
  PreferredAuthentications publickey
  IdentityFile ~/.ssh/example_rsa
